// ConsoleApplication1.cpp : 此檔案包含 'main' 函式。程式會於該處開始執行及結束執行。
//
//
#include <iostream>
#include <stdio.h>
//#include "Error.h"
//#include "Queue.h"
//
//
//stack stack_list_real;
//stack* stack_list = &stack_list_real;

int main()
{
    uint16_t value = 0;
    /*
    for (int i = 0; i < 5; i++)
    {
        push(10 + i, stack_list);
        printf("push = %d\n", 10 + i);

        for (int i = 0; i < max_size; i++)
        {
            printf("%d ", stack_list->buffer[i]);
        }
        printf("\n");
    }

    for (int i = 0; i < 5; i++)
    {
        pop(&value, stack_list);
        printf("pop = %d\n", value);

        for (int i = 0; i < max_size; i++)
        {
            printf("%d ", stack_list->buffer[i]);
        }
        printf("\n");
    }

    for (int i = 0; i < 5; i++)
    {
        push(9, stack_list);
        printf("push = %d\n", 9);

        for (int i = 0; i < max_size; i++)
        {
            printf("%d ", stack_list->buffer[i]);
        }
        printf("\n");
    }*/

    //system("PAUSE");
    //return 0;
}

